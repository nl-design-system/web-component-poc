import { Meta, Story, Canvas, ArgsTable, Description } from "@storybook/addon-docs/blocks";
import { argTypes, getParameters, decorators } from "./getStorySetup.js";
import { sanitize } from "dompurify";
import "../element.js";
import README from "../README.md";
import { MDXEmbedProvider } from "mdx-embed";

export const Template = ({ href, content }) => `<nl-backlink href="${href}">${sanitize(content)}</nl-backlink>`;

<Meta title="Web Components" argTypes={argTypes} parameters={getParameters(Template)} decorators={decorators} />

<Canvas>
  <Story name="Backlink" args={{ href: "#", content: "Back" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Backlink" />

<Description>{README}</Description>

## Design Kit

<MDXEmbedProvider>
  <Figma title="Backlink" url="file/BBZBJdbdVnvHwhcBoKsczw/components-backlink" />
</MDXEmbedProvider>
