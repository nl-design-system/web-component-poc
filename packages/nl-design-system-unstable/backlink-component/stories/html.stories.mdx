import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { argTypes, getParameters, decorators } from "./getStorySetup.js";
import { sanitize } from "dompurify";
import "../extend.js";

export const Template = ({ href, content }) => `<a is="nl-html-backlink" href="${href}">${sanitize(content)}</a>`;

<Meta title="HTML/Backlink" argTypes={argTypes} parameters={getParameters(Template)} decorators={decorators} />

# Backlink - HTML

<Canvas>
  <Story name="Simple" args={{ href: "#", content: "Back" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Simple" />

### With complex content

<Canvas>
  <Story
    name="Complex"
    args={{
      href: "http://example.com",
      content: "Back to <strong>example.com</strong>",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
